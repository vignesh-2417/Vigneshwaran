{
  "name": "salesforce-metadata-copilot",
  "private": true,
  "version": "0.1.0",
  "description": "Chrome extension assistant for Salesforce metadata requirements",
  "workspaces": [
    "shared",
    "extension",
    "backend"
  ],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w extension && npm run build -w backend",
    "test": "npm run test -w shared && npm run test -w extension && npm run test -w backend",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w extension && npm run typecheck -w backend"
  },
  "engines": {
    "node": ">=20"
  }
}
